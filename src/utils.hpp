#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <iomanip>
#include <fstream>
#include <string.h>

using namespace std;

static void readVocabularyImages(const string& filename, string& dirName, vector<string>& vocabularyFiles);

#endif
